import os
import json

SNAKEMAKE_DIR = os.path.dirname(workflow.snakefile)
WORKING_DIR = os.getcwd()

SAMPLES_BY_DATASET = json.load(open("data/samples.json", "r"))

rule all:
    input:
        "data/files_preprocessed.flag",
        "search/files_searched.flag"

include: "rules/preprocess_data.smk"
include: "rules/database_search.smk"
